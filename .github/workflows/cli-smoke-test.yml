name: CLI Smoke Test

on:
  push:
    branches: [ main ]
    paths:
      - 'packages/agentrejoin-cli/**'
      - 'packages/agentrejoin-server/**'
      - 'packages/agentrejoin-server-self-host/**'
      - 'packages/agentrejoin-wire/**'
      - '.github/workflows/cli-smoke-test.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'packages/agentrejoin-cli/**'
      - 'packages/agentrejoin-server/**'
      - 'packages/agentrejoin-server-self-host/**'
      - 'packages/agentrejoin-wire/**'
      - '.github/workflows/cli-smoke-test.yml'
  workflow_dispatch:

jobs:
  smoke-test-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 24]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    - name: Build packages
      run: |
        pnpm --filter agentrejoin-wire --fail-if-no-match build
        pnpm --filter agentrejoin --fail-if-no-match build

    # agentrejoin-server-self-host ships only a bundled runtime (no sources), and the
    # bundler is bun.
    - name: Install Bun
      uses: oven-sh/setup-bun@v2

    # `agentrejoin server` resolves the self-host runtime and bundled webapp from
    # agentrejoin-server-self-host. Its build bundles packages/agentrejoin-server's
    # standalone entrypoint and copies prisma/ in, so it must run before pack.
    - name: Build agentrejoin-server-self-host runtime + bundle webapp
      run: |
        pnpm --filter agentrejoin-server-self-host --fail-if-no-match build
        pnpm --filter agentrejoin-server-self-host --fail-if-no-match run bundle:webapp

    - name: Pack packages
      run: |
        pnpm --filter agentrejoin-wire --fail-if-no-match pack --pack-destination packages/agentrejoin-wire
        pnpm --filter agentrejoin --fail-if-no-match pack --pack-destination packages/agentrejoin-cli
        pnpm --filter agentrejoin-server-self-host --fail-if-no-match pack --pack-destination packages/agentrejoin-server-self-host

    - name: Install packed packages globally
      run: |
        WIRE_PACKAGE_FILE=$(ls packages/agentrejoin-wire/*.tgz)
        AGENTREJOIN_PACKAGE_FILE=$(ls packages/agentrejoin-cli/*.tgz)
        SERVER_PACKAGE_FILE=$(ls packages/agentrejoin-server-self-host/*.tgz)
        npm install -g "./$WIRE_PACKAGE_FILE" "./$SERVER_PACKAGE_FILE" "./$AGENTREJOIN_PACKAGE_FILE"

    - name: Test binary execution
      run: |
        # Test that the binary starts successfully
        echo "Testing agentrejoin --help..."
        timeout 30s agentrejoin --help || {
          echo "Error: agentrejoin --help failed or timed out"
          exit 1
        }

        echo "Testing agentrejoin --version..."
        timeout 10s agentrejoin --version || {
          echo "Error: agentrejoin --version failed or timed out"
          exit 1
        }

        echo "Testing agentrejoin doctor..."
        timeout 30s agentrejoin doctor || {
          echo "Error: agentrejoin doctor failed or timed out"
          exit 1
        }

        echo "Testing agentrejoin daemon status..."
        timeout 10s agentrejoin daemon status || {
          echo "Error: agentrejoin daemon status failed or timed out"
          exit 1
        }

        echo "Binary smoke test passed on Linux!"

    - name: Test packaged server (agentrejoin server)
      run: |
        export AGENTREJOIN_HOME_DIR="$(mktemp -d)"
        timeout 90s agentrejoin server --port 4505 --host 127.0.0.1 --no-persist --reset > /tmp/agentrejoin-server.log 2>&1 &
        SERVER_PID=$!
        UP=0
        for i in $(seq 1 60); do
          if curl -sf -m 2 http://127.0.0.1:4505/ -o /dev/null; then UP=1; echo "server up after ${i}s"; break; fi
          sleep 1
        done
        # Exercise the Prisma native query engine via a DB-backed read.
        PK=$(node -e "console.log(Buffer.alloc(32).toString('base64'))")
        BODY=$(curl -s -m 5 -G http://127.0.0.1:4505/v1/auth/request/status --data-urlencode "publicKey=$PK" || true)
        echo "auth/request/status -> $BODY"
        kill "$SERVER_PID" 2>/dev/null || true
        pkill -f agentrejoin-server || true
        echo "=== agentrejoin server log ==="; cat /tmp/agentrejoin-server.log || true
        if [ "$UP" != "1" ]; then echo "Error: agentrejoin server did not become healthy"; exit 1; fi
        if ! echo "$BODY" | grep -q '"status"'; then echo "Error: Prisma-backed endpoint did not respond"; exit 1; fi
        if grep -qiE 'PrismaClientInitializationError|could not locate.*Query Engine' /tmp/agentrejoin-server.log; then echo "Error: Prisma query engine failed to load"; exit 1; fi
        echo "agentrejoin server smoke test passed (booted + Prisma query engine OK)"

  smoke-test-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        node-version: [20, 24]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    - name: Build packages
      run: |
        pnpm --filter agentrejoin-wire --fail-if-no-match build
        pnpm --filter agentrejoin --fail-if-no-match build

    - name: Pack packages
      run: |
        pnpm --filter agentrejoin-wire --fail-if-no-match pack --pack-destination packages/agentrejoin-wire
        pnpm --filter agentrejoin --fail-if-no-match pack --pack-destination packages/agentrejoin-cli

    - name: Install packed package globally
      shell: cmd
      run: |
        for %%w in (packages\agentrejoin-wire\*.tgz) do for %%f in (packages\agentrejoin-cli\*.tgz) do npm install -g "%%w" "%%f"

    - name: Debug npm global installation structure
      shell: cmd
      run: |
        for /f "tokens=*" %%i in ('npm config get prefix') do set NPM_PREFIX=%%i
        echo NPM_PREFIX: %NPM_PREFIX%
        echo Listing npm prefix directory:
        dir "%NPM_PREFIX%" 2>nul || echo Failed to list NPM_PREFIX
        echo.
        echo Checking for node_modules in npm prefix:
        if exist "%NPM_PREFIX%\node_modules" (
          echo Found node_modules directory
          dir "%NPM_PREFIX%\node_modules" 2>nul
          echo.
          echo Checking for agentrejoin package:
          if exist "%NPM_PREFIX%\node_modules\agentrejoin" (
            echo Found agentrejoin package
            dir "%NPM_PREFIX%\node_modules\agentrejoin" 2>nul
            echo.
            echo Checking for dist directory:
            if exist "%NPM_PREFIX%\node_modules\agentrejoin\dist" (
              echo Found dist directory
              dir "%NPM_PREFIX%\node_modules\agentrejoin\dist" 2>nul
            ) else (
              echo No dist directory found
            )
          ) else (
            echo No agentrejoin package found
          )
        ) else (
          echo No node_modules directory found
        )

    - name: Test binary execution
      shell: cmd
      run: |
        @echo on

        rem Get npm global prefix and add to PATH
        for /f "tokens=*" %%i in ('npm config get prefix') do set NPM_PREFIX=%%i
        set PATH=%NPM_PREFIX%;%PATH%
        echo NPM_PREFIX: %NPM_PREFIX%
        echo Current PATH: %PATH%

        rem Debug: Check if agentrejoin exists in various locations
        echo Checking for agentrejoin binary...
        where agentrejoin 2>nul && echo Found agentrejoin in PATH || echo agentrejoin not found in PATH
        if exist "%NPM_PREFIX%\agentrejoin.cmd" echo Found agentrejoin.cmd in NPM_PREFIX
        if exist "%NPM_PREFIX%\node_modules\.bin\agentrejoin.cmd" echo Found agentrejoin.cmd in .bin directory
        dir "%NPM_PREFIX%\*agentrejoin*" 2>nul || echo No agentrejoin files found in NPM_PREFIX

        rem Debug: Check the actual contents and encoding of the installed file
        if exist "%NPM_PREFIX%\agentrejoin.cmd" (
          echo File size and type:
          dir "%NPM_PREFIX%\agentrejoin.cmd"
          echo.
          echo First few lines of the file:
          type "%NPM_PREFIX%\agentrejoin.cmd" | head -5
          echo.
          echo Hex dump of first 50 bytes to check line endings:
          powershell -Command "Get-Content '%NPM_PREFIX%\agentrejoin.cmd' -Encoding Byte -TotalCount 50 | ForEach-Object { '{0:X2}' -f $_ } | Join-String -Separator ' '"
          echo.
          echo Testing with direct path...
          "%NPM_PREFIX%\agentrejoin.cmd" --help
        ) else (
          echo Testing agentrejoin --help...
          agentrejoin --help
        )
        if errorlevel 1 (
          echo Error: agentrejoin --help failed
          exit /b 1
        )

        rem Test version
        if exist "%NPM_PREFIX%\agentrejoin.cmd" (
          "%NPM_PREFIX%\agentrejoin.cmd" --version
        ) else (
          agentrejoin --version
        )
        if errorlevel 1 (
          echo Error: agentrejoin --version failed
          exit /b 1
        )

        rem Test doctor
        echo Testing agentrejoin doctor...
        if exist "%NPM_PREFIX%\agentrejoin.cmd" (
          "%NPM_PREFIX%\agentrejoin.cmd" doctor
        ) else (
          agentrejoin doctor
        )
        if errorlevel 1 (
          echo Error: agentrejoin doctor failed
          exit /b 1
        )

        rem Test daemon status
        echo Testing agentrejoin daemon status...
        if exist "%NPM_PREFIX%\agentrejoin.cmd" (
          "%NPM_PREFIX%\agentrejoin.cmd" daemon status
        ) else (
          agentrejoin daemon status
        )
        if errorlevel 1 (
          echo Error: agentrejoin daemon status failed
          exit /b 1
        )

        echo Smoke test passed on Windows!

# We don't need a smoke test for macOS because most contributors are developing on macOS.
